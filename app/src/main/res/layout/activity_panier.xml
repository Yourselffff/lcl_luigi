<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- En-tête -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/tvPanierTitre"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Panier"
            android:textSize="24sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/tvNombreItems"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 film(s)"
            android:textSize="16sp"/>
    </LinearLayout>

    <!-- ListView pour afficher les films du panier -->
    <ListView
        android:id="@+id/lvPanier"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"/>

    <!-- Message si panier vide -->
    <TextView
        android:id="@+id/tvPanierVide"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Votre panier est vide"
        android:textSize="18sp"
        android:gravity="center"
        android:visibility="gone"/>

    <!-- Section prix total et boutons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="16dp">

        <TextView
            android:id="@+id/tvPrixTotal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Total: 0.00 €"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="end"
            android:layout_marginBottom="16dp"/>

        <Button
            android:id="@+id/btnViderPanier"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Vider le panier"
            android:onClick="onViderPanierClicked"
            android:backgroundTint="@android:color/darker_gray"
            android:layout_marginBottom="8dp"/>

        <Button
            android:id="@+id/btnValiderReservation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Valider la réservation"
            android:onClick="onValiderClicked"
            android:backgroundTint="@android:color/black"/>

        <Button
            android:id="@+id/btnContinuerAchats"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Continuer les achats"
            android:onClick="onContinuerAchatsClicked"
            android:layout_marginTop="8dp"/>

    </LinearLayout>

</LinearLayout>
